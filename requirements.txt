# NE PAS reinstaller torch : garder ton build PyTorch 2.7 / CUDA 12.8 existant.
# Z-Image n'est dans diffusers que depuis la source, d'ou le git+ ci-dessous.

# ATTENTION : le commit est EPINGLE volontairement.
# Un `git+.../diffusers` sans commit prend le HEAD du jour, ce qui n'est pas une
# borne mais une bombe a retardement : diffusers main est passe a
# huggingface-hub>=1.23 alors que transformers<5 exige huggingface-hub<1.0
# -> ResolutionImpossible a l'installation, pour tout le monde, du jour au
# lendemain. Ce commit est celui sur lequel CE repo a ete valide (il differe
# d'un fork a l'autre, cf. requirements-lock.txt).
# Pour tenter une version plus recente : remplacer le hash ci-dessous, relancer
# install.bat, et verifier que `transformers` s'installe encore.
git+https://github.com/huggingface/diffusers@2c7efb95349296cf6bcce981ea036275a82a94df

# transformers >= 4.51 requis : Z-Image Turbo utilise un encodeur Qwen3 (Qwen3Model),
# absent avant 4.51 (echec "module transformers has no attribute Qwen3Model").
# Borne <5 : la ligne 5.x a des changements d'API qui peuvent casser le diffusers
# source. Rester sur 4.51+ (teste OK en 4.57).
transformers>=4.51,<5
accelerate
sentencepiece
spandrel

# Required by diffusers to load LoRA weights (load_lora_weights / set_adapters).
# Without it: "PEFT backend is required for this method" -> LoRA silently skipped.
peft

# Comparateur avant/apres = composant HTML+JS interne (voir _make_compare_html dans app.py).
# Pas de dep externe -> stack stable.
# Borne <6: Gradio 6 a un middleware Brotli buggé avec h11 ("Too little data for
# declared Content-Length") qui spamme la console quand une reponse est interrompue.
gradio>=4.44,<6

pillow
numpy

# Optionnel: seulement pour le mode serveur persistant (--serve, palier 3).
fastapi
uvicorn

# Note xformers: si une version est deja installee mais buildee pour une autre version
# de torch que la tienne (ex xformers pour torch 2.9 alors que tu as 2.8), diffusers
# echoue avec "DLL load failed while importing _C" au chargement du VAE. Solution la
# plus simple: pip uninstall xformers. Torch 2.7+ a SDPA natif qui suffit.
